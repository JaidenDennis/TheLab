# NQ Agent — handoff

Last updated 2026-08-12. Branch `main`, 46 commits, 392 tests passing,
`ruff check` and `mypy --strict src/` clean. Verified on Windows and now on
macOS (this machine), both Python 3.12.

The machine migration this document was written for has happened: the repo
is cloned here with `origin` pointing at GitHub, and the environment below
is set up. The old bundle-based transfer instructions are gone with it —
push/pull through the remote from here on. Local commits exist that have
not been pushed; push when ready.

---

## Setting up on a new machine

```
git config core.autocrlf false          # see the warning below -- do this FIRST
uv venv --python 3.12
uv pip install -e ".[dev,live]"         # live = databento + aiohttp
uv run pytest -q                        # expect 392 passing
```

If `uv` is not installed: `python -m pip install uv`. Note it may land in a
Scripts directory that is not on PATH — `python -m uv` works regardless.

**Windows line endings.** `core.autocrlf` defaults to `true` on Windows, which
rewrites every file to CRLF on checkout, including the 5,041-line tick fixture.
Set it to `false` in the repo before doing anything else. This bit us once
already this session.

### What does NOT travel through git

All correctly gitignored, all need recreating:

- `.env` — secrets. Copy `.env.example` and fill in `NQ_DATABENTO_API_KEY`,
  `NQ_WEBHOOK_URL`, `NQ_WEBHOOK_TOKEN`.
- `var/` — journals and `state.db`. Regenerated by any run.
- `.venv/` — recreate with the commands above.

---

## Where things stand

Steps 1–10 of the original build sequence were the scaffolding (done before
this session). This session added, in order:

1. Two Windows-only defects fixed — the journal was silently losing records
   under concurrency, and sqlite connections were never explicitly closed.
2. **Phase A** — crash-safe teardown, `Executor.close()`, wiring-time
   validation, and the `FeedBinding` seam so a live feed no longer means
   editing `run_from_config`.
3. **Phase B** — the fill model now fills a gapped-through stop at the open;
   a backtester that runs the real engine and reports P&L off the journal;
   `OpeningRangeBreakout` as a reference strategy.
4. **Phase C** — `ReconnectingFeed` with backoff, `DatabentoFeed` adapter.
5. **Phase D** — tri-state `OrderOutcome`, `WebhookExecutor`.
6. **Phase E** — money-aware risk limits, `docs/RUNBOOK.md`.
7. Position reconciliation against broker truth.
8. Unrealised P&L risk, with a protective flatten on breach.

On this machine (2026-08-12), after the move:

9. The second whole-branch review ran (workflow-backed, high effort) and its
   findings were resolved — two real defects fixed (a carried position's
   since-entry move counting against a fresh daily limit; the risk-breach
   flatten dispatching from an unreconciled position belief), two findings
   documented as deliberate, test-pinned decisions, five cleanups applied.
10. Multi-day replay resume fixed: a killed replay now resumes the latest
    persisted session, not the fixture's first.

Read `docs/RUNBOOK.md` before running anything against an account. Its go-live
checklist has the blocking items marked.

## The next three things

1. **Get a Databento key and make `DatabentoFeed` connect for the first time.**
   Its SDK surface is verified against databento 0.83.0, but it has never sent
   a request. Expect to fix something. Pull `GLBX.MDP3` / schema `trades` /
   `stype_in=continuous` / symbol `NQ.c.0`, RTH windows (13:30–20:30 UTC).
   Pull one day first to calibrate size and cost.

2. **Write the real strategy.** This is the long pole and everything waits on
   it. Implement `Strategy`, add one line to `STRATEGIES` in `main.py`. Read
   `strategy/orb.py` as the worked example — it is a reference implementation,
   explicitly not a validated edge.

3. **Backtest it honestly** over months, with commission set to the real
   round-turn cost, developing on the first two thirds and testing the last
   third once.

## Two vendor-specific stubs left

Both are isolated and marked in their own files:

- `execution/webhook.py::_build_payload` — field names your broker expects.
- `reconcile.py::PositionSource` — needs an implementation against your
  broker's positions endpoint. Reconciliation is inert without one.

## Things known to be missing

See the end of `docs/RUNBOOK.md` for the full list. The ones that matter most:
unrealised P&L is marked on bar closes rather than tick by tick, and the
contract roll happens mid-session so do not hold through one. The
whole-branch review debt is paid (2026-08-12), though the fixes it produced
postdate it — keep the review-before-go-live checklist item honest about
whatever has changed since.

## A note on how this was verified

Every safety-critical change was mutation-tested: break the behaviour on
purpose, confirm a test goes red. That found three tests this session that
were passing for the wrong reason — including one where a drawdown test would
have passed whether or not the high-water mark logic existed at all. If you
add to the risk layer, do the same. The scratch scripts are gone, but the
pattern is: patch the source, run the suite, restore, assert it went red.
